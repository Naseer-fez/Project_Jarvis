# Intra-Batch Call Graph: Batch_02

```mermaid
graph TD;
  controller_v2 --> _setup_intent_routes;
  controller_v2 --> _dashboard_update;
  controller_v2 --> __init__;
  permission_matrix --> _parse_csv;
  profile --> _fresh_defaults;
  profile --> save;
  profile --> _load;
  state_machine --> get_valid_transitions;
  state_machine --> _notify;
  state_machine --> can_transition;
  state_machine --> force_idle;
  state_machine --> __exit__;
  state_machine --> __enter__;
  state_machine --> transition;
  synthesis --> _strip_wrappers;
  agent_loop --> _stop;
  agent_loop --> _plan_summary;
  agent_loop --> close;
  agent_loop --> _normalize_steps;
  agent_loop --> to_dict;
  agent_loop --> _fallback_reflection;
  agent_loop --> _ensure_thinking_state;
  agent_loop --> _truncate_obs;
  agent_loop --> _check_interrupt;
  live_automation --> _truncate;
  live_automation --> _normalize_text;
  live_automation --> _ensure_directories;
  live_automation --> _extract_command;
  live_automation --> _unique_path;
  live_automation --> _apply_scan_summary;
  live_automation --> _build_command_scan_batch;
  live_automation --> _handle_scan_failure;
  live_automation --> status;
  live_automation --> _save_state;
  live_automation --> _read_text_file;
  live_automation --> _append_log;
  live_automation --> _iso_now;
  live_automation --> _relocate;
  live_automation --> _move_to_failed;
  live_automation --> _cfg_int;
  live_automation --> _cfg_float;
  live_automation --> _extract_metadata_value;
  live_automation --> _cfg_bool;
  live_automation --> _remember_fingerprint;
  live_automation --> _build_ingest_scan_batch;
  live_automation --> _file_ready;
  live_automation --> _load_state;
  live_automation --> _fingerprint;
  payload_extractor --> _truncate;
  payload_extractor --> _normalize_text;
  payload_extractor --> extract_text_from_video;
  payload_extractor --> read_text_file;
  payload_extractor --> extract_text_from_image;
  rag_ingester --> chunk_text;
  scan_pipeline --> blank_scan_summary;
  autonomy_governor --> _is_known_tool;
  autonomy_governor --> _is_write_tool;
  goal_manager --> cancel;
  goal_manager --> _utcnow;
  goal_manager --> start;
  goal_manager --> to_dict;
  goal_manager --> resume;
  goal_manager --> pause;
  goal_manager --> fail;
  goal_manager --> get_goal;
  goal_manager --> complete;
  risk_evaluator --> label;
  risk_evaluator --> _load_config;
  risk_evaluator --> evaluate;
  risk_evaluator --> _parse;
  scheduler --> to_dict;
  scheduler --> get;
  scheduler --> mark_cancelled;
  scheduler --> _utcnow;
  base --> _first_non_empty;
  base --> _stringify_payload;
  context --> log;
  context --> get;
  context --> to_dict;
  context --> __exit__;
  complexity_scorer --> _structural_signals;
  goal_runner --> persist_goal_state;
  intents --> extract_goal_delay_seconds;
  intents --> parse_time_delay_with_parsedatetime;
  memory_subsystem --> _schedule_synthesis;
  memory_subsystem --> _track;
  request_rules --> is_explicit_web_search;
  request_rules --> clean;
  web_search --> _format_raw_fallback;
  actions --> _result;
  actions --> _stringify;
  actions --> _normalize_tool_result;
  actions --> requires_approval;
  actions --> _default_handlers;
  actions --> evaluate_risk;
  actions --> _audit;
  actions --> _contains_sensitive_text;
  contracts --> _new_id;
  contracts --> to_dict;
  mission --> close;
  mission --> to_dict;
  mission --> _audit;
  mission --> _summary_for;
  observation --> _result_success;
  observation --> _hash_path;
  observation --> _result_error;
  observation --> _target_from_payload;
  observation --> _result_payload;
  shortcuts --> _extract_search_query;
  shortcuts --> plan_desktop_command;
  shortcuts --> _supported_apps_message;
  engine --> register_rollback;
  engine --> publish_event;
  device_registry --> register_device;
  device_registry --> _load_from_config;
  serial_controller --> close;
  serial_controller --> connect;
  health --> _path_ready;
  health --> _ollama_check;
  health --> _collect_config_checks;
  health --> _config_get;
  health --> _config_get_bool;
  health --> _check_tesseract;
  health --> _module_available;
  client --> _walk;
  client --> _record_telemetry;
  client --> _get_workspace_map;
  client --> _messages_to_prompt;
  cloud_client --> _extract_openai_usage;
  model_router --> _pick_model_from_tier;
  model_router --> list_available_without_refresh;
  model_router --> _resolve_available_variant;
  model_router --> refresh_available_models;
  model_router --> route;
  model_router --> _cfg;
  model_router --> route_adaptive;
  model_router --> _ollama_base_url;
  model_router --> set_available_models;
  model_spec --> _parse_override;
  model_spec --> _load_config_overrides;
  model_spec --> get;
  ollama_client --> _normalize_base_url;
  ollama_client --> extract_model_names;
  ollama_client --> _strip_think;
  telemetry --> record;
  telemetry --> _resolve_cost_weight;
  telemetry --> _percentile;
  telemetry --> get_model_stats;
  logger --> verify;
  logger --> get;
  logger --> redact_sensitive_data;
  logger --> format;
  logger --> flush;
  logger --> setup;
  logger --> stop;
  logger --> _build_formatter;
  logger --> handle;
  logger --> _start_worker;
  logger --> _resolve_config_path;
  logger --> write;
  code_indexer --> _fallback_file_chunk;
  context_compressor --> _truncate;
  context_compressor --> _clean;
  context_compressor --> _estimate_tokens;
  context_compressor --> _get_item_text;
  context_compressor --> _compress_episodes;
  context_compressor --> _deduplicate;
  context_compressor --> _compress_conversations;
  context_compressor --> _compress_preferences;
  embeddings --> _get_word_vector;
  embeddings --> info;
  embeddings --> get_sentence_embedding_dimension;
  embeddings --> encode;
  embeddings --> _encode_single;
  hybrid_memory --> _looks_like_config;
  hybrid_memory --> _track_background_task;
  retriever --> query_tokens;
  retriever --> score_text;
  semantic_memory --> _collection;
  production --> _get;
  production --> is_production;
  production --> _get_bool;
  planner --> _strip_planner_artifacts;
  planner --> _build_prompt;
  planner --> _enrich_plan;
  planner --> _fallback_plan;
  planner --> _normalize_steps;
  planner --> _tool_schema;
  planner --> _parse_llm_plan;
  planner --> _clarification_plan;
  notifier --> notify;
  registry --> get;
  registry --> _record_to_observation;
  registry --> _build_desktop_action;
  auth --> hash_password;
  auth --> create_user;
  auth --> _token_hash;
  auth --> verify_password;
  auth --> _sign;
  auth --> user_count;
  auth --> _connect;
  auth --> _init_db;
  auto_clicker --> main;
  builtin_tools --> _fallback_classify_file;
  fast_search_tool --> should_skip;
  fast_search_tool --> search_file_content;
  fast_search_tool --> get_windows_drives;
  fast_search_tool --> run;
  fast_search_tool --> is_binary;
  gui_control --> _extract_json_object;
  gui_control --> _tool_result_payload;
  gui_control --> _validate_coords;
  gui_control --> _contains_sensitive_text;
  gui_control --> _runtime_config;
  gui_control --> _match_center;
  gui_control --> _require_pyautogui;
  hardware_tools --> _get_registry;
  screen --> _safe_int;
  screen --> _ensure_dir;
  screen --> find_text_on_screen;
  screen --> _clean_text;
  screen --> _ocr_words_from_data;
  screen --> read_screen_text;
  screen --> _ts;
  screen --> _capture_image;
  screen --> _safe_float;
  screen --> _match_entries;
  screen --> _require_pyautogui;
  screen --> _ocr_lines_from_words;
  universal_converter --> pdf_to_txt;
  universal_converter --> yaml_to_json;
  universal_converter --> txt_to_pdf;
  universal_converter --> csv_to_json;
  universal_converter --> json_to_csv;
  universal_converter --> txt_to_html;
  universal_converter --> convert_media_ffmpeg;
  universal_converter --> json_to_yaml;
  universal_converter --> perform_conversion;
  universal_converter --> convert_image;
  universal_converter --> data_to_excel;
  universal_converter --> md_to_html;
  universal_converter --> ensure_package;
  vision --> _get;
  vision --> _call_llava;
  web_tools --> _fallback_summary;
  web_tools --> _normalize_whitespace;
  web_tools --> _format_search_output;
  web_tools --> _needs_query_extraction;
  web_tools --> _provider_chain;
  web_tools --> _basic_query_cleanup;
  web_tools --> _load_default_config;
  web_tools --> from_sources;
  web_tools --> _get_service;
  web_tools --> _resolve;
  web_tools --> _clean_llm_line;
  audio --> load_audio;
  audio --> mel_filters;
  audio --> exact_div;
  audio_input --> _audio_input;
  audio_utils --> chunk_bytes_iter;
  audio_utils --> _ffmpeg_stream;
  audio_utils --> ffmpeg_microphone;
  audio_utils --> _get_microphone_name;
  stt --> _init;
  stt --> _get;
  stt --> _choose_backend;
  tts --> _split_sentences;
  tts --> stop;
  tts --> _engine_chain;
  tts --> _get;
  tts --> _speak_sentence;
  tts --> _init_backend;
  wake_word --> _get;
```
