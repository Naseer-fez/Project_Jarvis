# JARVIS Session 5 - Full Requirements
# All packages run 100% LOCAL — no cloud APIs

# ── Core ──────────────────────────────────────────────
httpx>=0.27.0            # Async HTTP for Ollama API calls
asyncio-throttle>=1.0.2  # Rate limiting

# ── Memory ────────────────────────────────────────────
chromadb>=0.4.0          # Vector DB for semantic search
sentence-transformers>=2.7.0  # all-MiniLM-L6-v2 embeddings

# ── Voice: STT ───────────────────────────────────────
openai-whisper>=20231117  # Local speech-to-text (base/tiny model)
pyaudio>=0.2.14          # Microphone input

# ── Voice: Wake Word ─────────────────────────────────
pvporcupine>=3.0.0       # Offline "Jarvis" wake word detection

# ── Voice: TTS ───────────────────────────────────────
pyttsx3>=2.90            # Fallback TTS (no model needed)
# piper-tts              # Install separately: https://github.com/rhasspy/piper

# ── ML / Torch ───────────────────────────────────────
torch>=2.0.0             # Required by whisper & sentence-transformers
torchvision              # Optional, but often pulled in
torchaudio               # Required by whisper

# ── Utilities ────────────────────────────────────────
numpy>=1.24.0
Pillow>=10.0.0
python-dotenv>=1.0.0
