# `template.py` - API Analyst Report

## Overview
A reference template for future integrations showing the new plugin contract.

## Endpoints / Tools
- None

## External Contracts / Dependencies
- None

## Assumptions
- `is_available` returns `False` by default so it is never registered as a real plugin.
- Returns empty list for tools.
